class EvmCloud < Formula
  desc "CLI for deploying EVM blockchain data infrastructure"
  homepage "https://github.com/ExoMonk/evm-cloud"
  license "Apache-2.0"

  # This file is a placeholder in this repository.
  #
  # The release pipeline generates the real formula with pinned version + checksums
  # from release artifacts via:
  #   scripts/generate-homebrew-formula.sh <version-tag> <checksums-file> <output>
  #
  # The generated formula is published to the Homebrew tap repository
  # (`ExoMonk/homebrew-tap`) by the CLI release workflow.
end
