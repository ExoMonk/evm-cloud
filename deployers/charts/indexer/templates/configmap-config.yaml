apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "indexer.fullname" . }}-config
  labels:
    app.kubernetes.io/name: {{ include "indexer.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  rindexer.yaml: |
{{ .Values.config.rindexerYaml | indent 4 }}
