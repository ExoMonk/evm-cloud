apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rpc-proxy.fullname" . }}-config
  labels:
    app.kubernetes.io/name: {{ include "rpc-proxy.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  erpc.yaml: |
{{ .Values.config.erpcYaml | indent 4 }}
